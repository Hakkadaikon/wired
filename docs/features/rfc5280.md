[Docs](../README.md) › [Features](README.md) › RFC 5280

# RFC 5280 — Internet X.509 PKI Certificate and CRL Profile

EARS requirement ledger extracted from the spec text
(`tasks/specs/rfc5280.txt`, not in git). Each requirement carries the test
that demonstrates it; an unchecked box with no test line is an open gap.
Scope: what this TLS server SDK consumes — certificate DER parsing and
certification path validation. Status as of 2026-07.

Legend:

- `[x]` — demonstrated by the referenced test
- `[~]` — exercised indirectly (evidence line explains how; no dedicated test)
- `[ ]` — not demonstrated by any test yet

**Coverage: 35/44 tested, 9 indirect, 0 untested.**

## §4.1 Basic Certificate Fields

- [x] 5280-001 (§4.1) The implementation shall parse a certificate as a DER
  SEQUENCE of tbsCertificate, signatureAlgorithm, and signatureValue.
  - test: `tests/crypto/x509_test.c` — `test_x509_parse_golden`
  - test: `tests/crypto/fields_test.c` — `test_fields_golden`
- [x] 5280-002 (§4.1) If the certificate encoding is truncated or not a valid
  DER SEQUENCE, then the implementation shall reject the parse.
  - test: `tests/crypto/x509_test.c` — `test_x509_truncated`
  - test: `tests/crypto/x509_test.c` — `test_x509_not_seq`
  - test: `fuzz/fuzz_x509.c` — `LLVMFuzzerTestOneInput`
- [x] 5280-003 (§4.1.1.2) If the signatureAlgorithm field differs from the
  signature field inside tbsCertificate, then the implementation shall
  reject the certificate.
  - test: `tests/crypto/chainverify_test.c` — `test_sigalg_mismatch_fails`
  - test: `tests/crypto/sigalg_test.c` — `test_sigalg_matches`
- [x] 5280-004 (§4.1.1.3) The implementation shall verify the signatureValue
  over the ASN.1 DER-encoded tbsCertificate bytes.
  - test: `tests/crypto/chainverify_test.c` — `test_leaf_signed_by_root`
  - test: `tests/crypto/chainverify_test.c` — `test_tampered_tbs_fails`
- [x] 5280-005 (§4.1.1.2) If the signature algorithm OID is not in the
  supported set, then the implementation shall reject the certificate.
  - test: `tests/crypto/sigalgoid_test.c` — `test_sigalgoid_unknown`
  - test: `tests/crypto/rsachain_test.c` — `test_rsachain_sha224_rejected`

## §4.1.2 TBSCertificate

- [x] 5280-006 (§4.1.2.1) The implementation shall recognize version 3
  certificates.
  - test: `tests/crypto/version_serial_test.c` — `test_vs_golden`
- [x] 5280-007 (§4.1.2.1) When the version field is absent, the
  implementation shall treat the certificate as version 1.
  - test: `tests/crypto/version_serial_test.c` — `test_vs_default_v1`
- [~] 5280-008 (§4.1.2.1) The implementation shall be prepared to accept a
  certificate of any version.
  - evidence: the TBS walk skips the `[0]` version element without
    constraining its value; only v1-default and v3 fixtures are asserted.
- [x] 5280-009 (§4.1.2.2) The implementation shall handle serialNumber
  values up to 20 octets.
  - test: `tests/crypto/version_serial_test.c` — `test_vs_golden`
  - test: `tests/crypto/version_serial_test.c` — `test_vs_serial_too_long`
- [x] 5280-010 (§4.1.2.4) The implementation shall process the issuer and
  subject distinguished name fields to perform name chaining for path
  validation.
  - test: `tests/crypto/chain_test.c` — `test_self_signed_issuer_equals_subject`
  - test: `tests/crypto/chain_test.c` — `test_dn_is_sequence_tlv`
  - test: `tests/crypto/castore_test.c` — `test_add_then_find_by_leaf_issuer`
- [~] 5280-011 (§4.1.2.4) The implementation shall be prepared to receive
  issuer and subject names containing the standard attribute types.
  - evidence: DNs are handled as opaque DER byte spans, so any attribute
    type passes through unchanged; no per-attribute decoding or test.
- [x] 5280-012 (§4.1.2.5) The implementation shall process validity dates
  encoded in either UTCTime or GeneralizedTime.
  - test: `tests/crypto/validity_test.c` — `test_validity_golden`
  - test: `tests/crypto/rsachain_test.c` — `test_gentime_validity`
- [x] 5280-013 (§4.1.2.5) The implementation shall treat the validity period
  as the time from notBefore through notAfter, inclusive.
  - test: `tests/crypto/validity_test.c` — `test_validity_golden`
- [x] 5280-014 (§4.1.2.5.1) The implementation shall interpret UTCTime
  values as YYMMDDHHMMSSZ, expressed in Greenwich Mean Time and including
  seconds.
  - test: `tests/crypto/validity_test.c` — `test_utctime_encode_known`
  - test: `tests/crypto/validity_test.c` — `test_utctime_encode_roundtrip`
- [~] 5280-015 (§4.1.2.5.1) The implementation shall interpret a two-digit
  year YY as 20YY where YY is less than 50, and as 19YY otherwise.
  - evidence: the 20YY branch is exercised by the 2026 golden fixture
    (`test_validity_golden`); the 19YY decode branch has no test.
- [x] 5280-016 (§4.1.2.5.2) The implementation shall interpret
  GeneralizedTime values as YYYYMMDDHHMMSSZ with no fractional seconds.
  - test: `tests/crypto/rsachain_test.c` — `test_gentime_validity`
- [x] 5280-017 (§4.1.2.7) The implementation shall extract the public key
  algorithm and subjectPublicKey from the subjectPublicKeyInfo field.
  - test: `tests/crypto/spki_test.c` — `test_spki_golden`
  - test: `tests/crypto/ec_pubkey_test.c` — `test_ec_pubkey_extract`
  - test: `tests/crypto/rsa_pubkey_test.c` — `test_rsa_pubkey_extract`
  - test: `tests/crypto/p384chain_test.c` — `test_p384_named_curve`
  - test: `tests/crypto/p384chain_test.c` — `test_p384_ec_pubkey384`
- [x] 5280-018 (§4.1.2.8) The implementation shall be capable of parsing
  certificates that include unique identifiers.
  - test: `tests/crypto/x509_test.c` —
    `test_unique_id_issuer_only_reaches_extensions`
  - test: `tests/crypto/x509_test.c` — `test_unique_id_both_reaches_extensions`
  - test: `tests/crypto/x509_test.c` —
    `test_unique_id_subject_only_reaches_extensions`

## §4.2 Certificate Extensions

- [x] 5280-019 (§4.2) The implementation shall process a recognized
  extension, located by its extnID, whether or not it is marked critical.
  - test: `tests/crypto/basicconstraints_test.c` — `test_ca_true`
  - test: `tests/crypto/san_test.c` — `test_exact_match`
- [x] 5280-020 (§4.2) If a certificate contains an unrecognized critical
  extension, then the implementation shall reject the certificate.
  - test: `tests/crypto/x509_test.c` — `test_unknown_critical_rejects`
  - test: `tests/crypto/x509_test.c` — `test_unknown_critical_mixed_rejects`
  - test: `tests/crypto/x509_test.c` — `test_unknown_critical_default_false`
- [x] 5280-021 (§4.2.1.1, §4.2.1.2) The implementation shall recognize the
  authority and subject key identifier extensions.
  - test: `tests/crypto/keyid_test.c` — `test_skid_found`
  - test: `tests/crypto/keyid_test.c` — `test_akid_found`
- [x] 5280-022 (§4.2.1.3) If the key usage extension is present, then the
  implementation shall not use the subject public key to verify certificate
  signatures unless the keyCertSign bit is set (also §6.1.4 (n)).
  - test: `tests/crypto/keyusage_test.c` — `test_keyusage`
- [x] 5280-023 (§4.2.1.4) If a critical certificate policies extension
  cannot be interpreted, then the implementation shall reject the
  certificate.
  - test: `tests/crypto/x509_test.c` —
    `test_critical_certificate_policies_rejects`
  - evidence: certificatePolicies is absent from `known_ext_oids`, so
    `x509_has_unknown_critical`'s existing rejection already covers
    this; the test above pins the behavior as a regression guard.

## §4.2.1.6 Subject Alternative Name

- [x] 5280-024 (§4.2.1.6) The implementation shall match a presented DNS
  hostname against the dNSName entries of the subject alternative name
  extension, attaching no significance to case.
  - test: `tests/crypto/san_test.c` — `test_exact_match`
  - test: `tests/crypto/san_test.c` — `test_san_case_fold`
  - test: `tests/crypto/san_test.c` — `test_no_match`
  - test: `tests/tls/fullhs_policy_test.c` — `test_fullhs_policy_san`
- [x] 5280-025 (§4.2.1.6) When the subject alternative name extension is
  absent, the implementation shall not accept a dNSName identity for the
  certificate.
  - test: `tests/crypto/san_test.c` — `test_no_san`
  - test: `tests/tls/fullhs_policy_test.c` — `test_fullhs_policy_no_san`

## §4.2.1.9 Basic Constraints

- [x] 5280-026 (§4.2.1.9) The implementation shall treat the certified
  public key as usable for verifying certificate signatures only when the
  basic constraints cA boolean is asserted.
  - test: `tests/crypto/basicconstraints_test.c` — `test_ca_true`
  - test: `tests/crypto/basicconstraints_test.c` — `test_ca_false`
- [x] 5280-027 (§4.2.1.9) If the basic constraints extension is absent or
  the cA boolean is not asserted, then the implementation shall not use the
  certified public key to verify certificate signatures.
  - test: `tests/crypto/basicconstraints_test.c` — `test_no_extensions`
  - test: `tests/crypto/pathvalidate_test.c` — `test_non_ca_intermediate_fails`
- [x] 5280-028 (§4.2.1.9) The implementation shall limit the number of
  intermediate certificates following a CA certificate to its
  pathLenConstraint value.
  - test: `tests/crypto/basicconstraints_test.c` — `test_pathlen_allows_boundary`
  - test: `tests/crypto/basicconstraints_test.c` — `test_pathlen_allows_one`
  - test: `tests/crypto/pathvalidate_test.c` — `test_pathlen_zero_direct_leaf_ok`
  - test: `tests/crypto/pathvalidate_test.c` — `test_pathlen_zero_sub_ca_fails`
- [x] 5280-029 (§4.2.1.9) If the pathLenConstraint field is negative or
  malformed, then the implementation shall reject the certificate.
  - test: `tests/crypto/basicconstraints_test.c` — `test_pathlen_negative_rejected`
  - test: `tests/crypto/basicconstraints_test.c` — `test_pathlen_not_integer_rejected`
- [~] 5280-030 (§4.2.1.9) Where pathLenConstraint does not appear, the
  implementation shall impose no path length limit.
  - evidence: `x509_pathlen_allows` returns allow when no INTEGER
    follows cA; exercised by the pathLen-free golden root in
    `test_valid_chain`, but no dedicated absent-pathLen assertion.

## §4.2.1.10–4.2.1.14 Constraint extensions

- [x] 5280-031 (§4.2.1.10) The implementation shall process name constraints
  imposed on the directoryName form or reject the certificate.
  - test: `tests/crypto/nameconstraints_test.c` —
    `test_nc_permitted_exact_match_ok`
  - test: `tests/crypto/nameconstraints_test.c` — `test_nc_permitted_child_ok`
  - test: `tests/crypto/nameconstraints_test.c` —
    `test_nc_excluded_match_rejected`
  - test: `tests/crypto/pathvalidate_test.c` —
    `test_name_constraints_permitted_subject_ok`
  - test: `tests/crypto/pathvalidate_test.c` —
    `test_name_constraints_excluded_subject_rejects`
- [x] 5280-032 (§4.2.1.11) The implementation shall be able to process the
  requireExplicitPolicy field of the policy constraints extension.
  - test: `tests/crypto/policyconstraints_test.c` — `test_reqexp_value_read`
  - test: `tests/crypto/policyconstraints_test.c` —
    `test_reqexp_malformed_rejects`
  - test: `tests/crypto/pathvalidate_test.c` —
    `test_require_explicit_policy_matching_policy_ok`
  - test: `tests/crypto/pathvalidate_test.c` —
    `test_require_explicit_policy_disjoint_policy_rejects`
- [~] 5280-033 (§4.2.1.12) If an extended key usage extension is present,
  then the implementation shall use the certificate only for a purpose
  consistent with both the key usage and extended key usage extensions.
  - evidence: `x509_eku_allows` (`src/crypto/pki/encoding/x509/eku.c`)
    is wired into `castore_validate_chain` requiring the leaf to
    permit id-kp-serverAuth when extKeyUsage is present.
  - test: `tests/crypto/eku_test.c` — `test_eku`
  - gap: only the extKeyUsage-vs-purpose check is wired; cross-consistency
    with the keyUsage bits (e.g. digitalSignature for TLS) is not checked.
- [~] 5280-034 (§4.2.1.14) The implementation shall recognize the inhibit
  anyPolicy extension and stop treating anyPolicy as an explicit match
  after the indicated number of certificates.
  - evidence: `x509_inhibit_any_policy`
    (`src/crypto/pki/encoding/x509/policyconstraints.c`) reads the
    extension's SkipCerts value, and `pathvalidate.c`'s `apply_inhibit_any`
    lowers a running counter that is passed to `x509_policy_tree_fold`
    as `any_inhibited` on every path-validation call.
  - test: `tests/crypto/policyconstraints_test.c` —
    `test_inhibit_any_value_read`
  - test: `tests/crypto/policytree_test.c` —
    `test_tree_fold_any_policy_inhibited_narrows`
  - test: `tests/crypto/policytree_test.c` —
    `test_tree_fold_any_and_specific_inhibited_keeps_specific`
  - gap: the counter-decrement-to-zero wiring in `pathvalidate.c` has no
    dedicated integration test exercising an actual inhibitAnyPolicy
    extension through `castore_validate_chain`.

## §6.1 Basic Path Validation

- [x] 5280-035 (§6.1.3 (a)(1)) The implementation shall verify each
  certificate's signature using the working public key of its issuer.
  - test: `tests/crypto/pathvalidate_test.c` — `test_valid_chain`
  - test: `tests/crypto/pathvalidate_test.c` — `test_tampered_signature_fails`
  - test: `tests/crypto/p384chain_test.c` — `test_p384_full_chain`
  - test: `tests/crypto/rsachain_test.c` — `test_rsachain_sha256`
- [~] 5280-036 (§6.1, §6.1.3 (a)(2)) The implementation shall verify that
  each certificate in the path is valid at the time of evaluation.
  - evidence: the leaf window is enforced through the handshake acceptance
    policy (`tests/tls/fullhs_policy_test.c` — `test_fullhs_policy_validity`);
    intermediate and root windows are not checked in
    `castore_validate_chain`.
- [x] 5280-037 (§6.1.3 (a)(4)) The implementation shall verify that each
  certificate's issuer name equals the subject name of the next certificate
  in the path (the working issuer name).
  - test: `tests/crypto/pathvalidate_test.c` — `test_name_mismatch_fails`
  - test: `tests/tls/fullhs_chain_test.c` — `test_fullhs_castore_swapped`
- [x] 5280-038 (§6.1) The implementation shall accept a certification path
  only when it chains to a registered trust anchor.
  - test: `tests/crypto/pathvalidate_test.c` — `test_unregistered_root_fails`
  - test: `tests/crypto/castore_test.c` — `test_find_unknown_subject_fails`
  - test: `tests/tls/fullhs_chain_test.c` — `test_fullhs_castore_ok`
  - test: `tests/tls/fullhs_chain_test.c` — `test_fullhs_castore_wrong_root`
- [x] 5280-039 (§6.1.1 (d)) When trust anchor information is provided as a
  self-signed certificate, the implementation shall use its subject as the
  trusted issuer name and its subjectPublicKeyInfo as the trusted key.
  - test: `tests/crypto/castore_test.c` — `test_add_then_find_by_leaf_issuer`
  - test: `tests/crypto/chainverify_test.c` — `test_root_self_signature`
  - test: `tests/crypto/pathvalidate_test.c` — `test_lone_root_chain`
- [x] 5280-040 (§6.1.4 (k)) If an intermediate certificate does not assert
  basic constraints cA TRUE, then the implementation shall reject the path.
  - test: `tests/crypto/pathvalidate_test.c` — `test_non_ca_intermediate_fails`
- [x] 5280-041 (§6.1) The implementation shall not accept a path in which
  the same certificate appears more than once.
  - test: `tests/crypto/pathvalidate_test.c` — `test_duplicate_cert_fails`
  - test: `tests/crypto/pathvalidate_test.c` —
    `test_duplicate_cert_at_tail_fails`
- [x] 5280-042 (§6.1) The implementation shall not count self-issued
  certificates when evaluating path length.
  - test: `tests/crypto/pathvalidate_test.c` —
    `test_self_issued_excluded_from_pathlen`
- [~] 5280-043 (§6.1.2–6.1.5) Where certificate policies are processed, the
  implementation shall construct the valid_policy_tree through the
  initialization, per-certificate, and wrap-up steps.
  - evidence: `x509_policy_tree_init`/`x509_policy_tree_fold`/
    `x509_policy_tree_nonempty`
    (`src/crypto/pki/encoding/x509/policytree.c`) are wired into
    `pathvalidate.c`'s `policy_processing_ok`, walked in path order from
    the trust anchor's issued certificate to the leaf on every
    `castore_validate_chain` call.
  - test: `tests/crypto/policytree_test.c` —
    `test_tree_fold_specific_narrows_nonempty`
  - test: `tests/crypto/policytree_test.c` —
    `test_tree_fold_disjoint_policies_empties`
  - test: `tests/crypto/certpolicies_test.c` — `test_cp_two_policies`
  - test: `tests/crypto/pathvalidate_test.c` —
    `test_require_explicit_policy_matching_policy_ok`
  - gap: the tree is a flat set-of-OIDs approximation, not RFC 5280's
    node/qualifier tree; policy mapping (§4.2.1.5) is not processed, and
    user-initial-policy-set is fixed to anyPolicy (no caller passes an
    explicit initial policy set). The trust anchor's own
    certificatePolicies extension is excluded from evaluation, matching
    §6.1.3's "for certificates 1 through n" scope.

## §7.1 Distinguished name comparison

- [~] 5280-044 (§7.1) The implementation shall compare DirectoryString name
  attributes using the RFC 4518 string preparation algorithm with
  caseIgnoreMatch.
  - evidence: `quic_x509_dirstring_eq`
    (`src/crypto/pki/encoding/x509/dirstring.c`) implements a simplified
    RFC 4518 preparation (case folding, leading/trailing trim, internal
    whitespace collapse) as an additional comparison API; the existing
    byte-exact `x509_dn_equal` is unchanged and remains the DN
    comparison used by path validation's name chaining.
  - test: `tests/crypto/dirstring_test.c` — `test_dirstring_case_insensitive`
  - test: `tests/crypto/dirstring_test.c` —
    `test_dirstring_internal_space_collapses`
  - test: `tests/crypto/dirstring_test.c` —
    `test_dirstring_leading_trailing_trimmed`
  - test: `tests/crypto/dirstring_test.c` — `test_dn_ci_case_insensitive`
  - gap: `quic_x509_dirstring_eq` is not wired into
    `castore_validate_chain`'s name chaining (`names_chain` still uses
    byte-exact `x509_dn_equal`); re-encoded or case-folded issuer/
    subject DNs across a real chain are not exercised end-to-end.

## Out of scope

Client-only, CA/operator-level, or deliberately unshipped requirements this
server SDK does not implement, excluded from the coverage denominator:

- (§2–§3) Requirements, assumptions, and overview of approach —
  informational.
- (§3.3, §5, §5.2, §5.3) CRL profile, CRL extensions, and CRL entry
  extensions — no CRL code in `src/`.
- (§6.1.3 (a)(3), §6.3) Revocation status determination and CRL validation —
  no revocation mechanism shipped.
- (§4.1.2.2, §4.1.2.4, §4.1.2.5, §4.1.2.6, §4.2) Conforming-CA issuance
  requirements (serial assignment, DirectoryString encoding choices,
  UTCTime-through-2049 encoding, criticality markings) — CA/operator
  behavior, not certificate consumption.
- (§4.2.1.5) Policy mappings — optional application feature, not shipped.
- (§4.2.1.7) Issuer alternative name — not processed in path validation per
  the spec itself.
- (§4.2.1.8) Subject directory attributes — no relying-party processing
  requirement.
- (§4.2.1.13, §4.2.1.15) CRL distribution points and freshest CRL — CRL
  retrieval machinery, not shipped.
- (§4.2.2.1, §4.2.2.2) Authority and subject information access (OCSP,
  caIssuers) — no online retrieval code.
- (§4.2.1.6) Wildcard name semantics — not addressed by RFC 5280; delegated
  to the RFC 6125 host-identity rules.
- (§4.2.1.6) Matching of non-dNSName SAN forms (rfc822Name, iPAddress,
  URI) — the SDK consumes DNS hostname identities only.
- (§7.2–§7.5) Internationalized domain names, IRIs, and mail addresses —
  no IDN processing consumed.
- (§8) Security considerations — informational guidance.
