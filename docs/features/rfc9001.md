[Docs](../README.md) › [Features](README.md) › RFC 9001

# RFC 9001 — Using TLS to Secure QUIC

EARS requirement ledger extracted from the spec text
(`tasks/specs/rfc9001.txt`, not in git). Each requirement carries the test
that demonstrates it; an unchecked box with no test line is an open gap.
Status as of 2026-07.

Legend:

- `[x]` — demonstrated by the referenced test
- `[~]` — exercised indirectly (evidence line explains how; no dedicated test)
- `[ ]` — not demonstrated by any test yet

**Coverage: 49/68 tested, 8 indirect, 11 untested.**

## §4 Carrying TLS Messages

- [x] 9001-001 (§4) The server shall carry TLS handshake data in CRYPTO
  frames, each a contiguous block identified by an offset and length.
  - test: `tests/transport/crypto_stream_test.c` — `test_emit_splits`
  - test: `tests/transport/crecv_collect_test.c` — `test_crecv_single_offset0`
- [~] 9001-002 (§4) When retransmitting CRYPTO data, the server shall use the
  same keys (encryption level) originally used for that data.
  - evidence: send history and retransmission state are kept per packet-number
    space (`tests/transport/pnspaces_sent_test.c` —
    `test_pnspaces_sent_ack_stays_in_space`), so CRYPTO retransmissions are
    rebuilt in their original space; no dedicated cross-level test.
- [x] 9001-003 (§4) When packets of different types need to be sent, the
  server shall coalesce them into the same UDP datagram, long headers before
  the final short-header packet.
  - test: `tests/transport/coalesce_test.c` — `test_coalesce_split`
  - test: `tests/transport/coalorder_test.c` — `test_coalorder_short_last_only`
- [x] 9001-004 (§4, Table 1) The server shall map each encryption level to
  its packet type and packet number space (Initial, 0-RTT/1-RTT → application
  data, Handshake).
  - test: `tests/transport/ptype_test.c` — `test_ptype_each`
  - test: `tests/transport/pnspace_test.c` — `test_pnspace_independent`

## §4.1 Interface to TLS

- [x] 9001-005 (§4.1.1) The server shall consider the handshake complete only
  when it has both sent its Finished message and verified the peer's Finished
  message.
  - test: `tests/tls/hsdone_test.c` — `test_hsdone`
  - test: `tests/tls/hsdriver_test.c` — `test_not_confirmed_before_complete`
- [x] 9001-006 (§4.1.2) When the handshake completes, the server shall send a
  HANDSHAKE_DONE frame as soon as possible, exactly once.
  - test: `tests/tls/srvfin_hsdone_test.c` — `test_srvfin_hsdone`
- [x] 9001-007 (§4.1.3) The server shall not produce handshake bytes before
  it has been provided the client's ClientHello bytes.
  - test: `tests/tls/hsdriver_test.c` —
    `test_server_does_not_send_before_clienthello`
  - test: `tests/tls/server_test.c` — `test_server_flight_before_ch`
- [x] 9001-008 (§4.1.3) The server shall send each handshake message in a
  CRYPTO frame at the encryption level defined for that message.
  - test: `tests/tls/hsdriver_test.c` — `test_message_carried_at_defined_level`
- [x] 9001-009 (§4.1.3) When CRYPTO frames arrive out of order, the server
  shall buffer them and deliver the data to TLS in offset order.
  - test: `tests/transport/crecv_collect_test.c` — `test_crecv_out_of_order`
  - test: `tests/transport/crypto_stream_test.c` — `test_recv_reorder_dup`
- [ ] 9001-010 (§4.1.3) If a packet from a previously installed encryption
  level contains CRYPTO data extending past the end of previously received
  data in that flow, then the server shall treat it as a connection error of
  type PROTOCOL_VIOLATION.
- [ ] 9001-011 (§4.1.3) If data from a previous encryption level remains
  unconsumed when TLS provides keys for a higher level, then the server shall
  treat it as a connection error of type PROTOCOL_VIOLATION.
- [~] 9001-012 (§4.1.4) While keys for an encryption level are not yet
  available, the server should buffer packets that might be processed with
  those keys.
  - evidence: interop testcase `zerortt` (see
    [test coverage](../test-coverage.md)) — 0-RTT datagrams arriving before
    their keys exist are buffered at the boot-accumulator layer and replayed
    once keys are derived; no dedicated unit test.
- [~] 9001-013 (§4.2) If a version of TLS older than 1.3 is negotiated, then
  the server shall terminate the connection.
  - evidence: the server only ever offers and selects TLS 1.3 (0x0304) in
    `supported_versions` (`src/tls/handshake/roles/shbuild/shbuild.c`), so an
    older version can never be negotiated; no dedicated rejection test.

## §4.4 Peer Authentication

- [~] 9001-014 (§4.4) The server shall not send post-handshake TLS
  CertificateRequest messages.
  - evidence: the only CertificateRequest builder is the handshake-flight one
    (`tests/crypto/certreq_test.c` covers its wire form); no post-handshake
    send path exists.

## §4.5 Session Resumption

- [x] 9001-015 (§4.5) Where session resumption is enabled, the server shall
  issue NewSessionTicket messages after the handshake completes and shall
  accept a valid ticket-derived PSK in a subsequent handshake.
  - test: `tests/tls/newsessionticket_test.c` — `test_nst_roundtrip`
  - test: `tests/tls/sdrv_test.c` —
    `test_sdrv_psk_valid_ticket_and_binder_accepted`
  - test: `tests/tls/server_test.c` —
    `test_server_psk_accepted_keys_match_client`

## §4.6 0-RTT

- [x] 9001-016 (§4.6.1) The server shall not send the early_data extension in
  a NewSessionTicket with a max_early_data_size other than 0xffffffff.
  - test: `tests/tls/newsessionticket_test.c` — `test_nst_early_data_ext`
- [x] 9001-017 (§4.6.2) When the server accepts 0-RTT, it shall send the
  early_data extension in EncryptedExtensions and derive the early traffic
  keys.
  - test: `tests/tls/eebuild_test.c` — `test_eebuild_early_data_accepted`
  - test: `tests/tls/sdrv_test.c` —
    `test_sdrv_early_data_accepted_derives_keys`
- [x] 9001-018 (§4.6.2) When rejecting 0-RTT, the server shall not process
  any 0-RTT packets.
  - test: `tests/tls/sdrv_test.c` — `test_sdrv_psk_without_early_data_no_0rtt`
  - test: `tests/tls/ticketguard_test.c` — `test_zerortt_accept_rule`
- [x] 9001-019 (§4.6.3) When a ClientHello carries the early_data extension,
  the server shall reject 0-RTT unless the transport and application
  configuration associated with the resumed session is compatible with its
  current configuration.
  - test: `tests/tls/zerortt_params_test.c` — `test_zerortt_params`
  - test: `tests/tls/resume_test.c` — `test_resume`
  - test: `tests/tls/ticketversion_test.c` — `test_ticket_compatible_version`

## §4.7 HelloRetryRequest

- [x] 9001-020 (§4.7) Where address verification is needed, the server should
  use the Retry feature rather than HelloRetryRequest.
  - test: `tests/transport/dosmitigate_test.c` — `test_dosmitigate`
  - test: `tests/tls/retry_drive_test.c` — `test_retry_drive`

## §4.8 TLS Errors

- [x] 9001-021 (§4.8) When TLS raises an alert, the server shall convert it
  to a QUIC error code by adding the AlertDescription to 0x0100 (the
  CRYPTO_ERROR range), sent in a CONNECTION_CLOSE frame of type 0x1c.
  - test: `tests/common/error_test.c` — `test_error_crypto`
- [~] 9001-022 (§4.8) The server shall treat every TLS alert as if it were at
  the "fatal" level.
  - evidence: the alert conversion (`src/common/diag/error/error.c`) has no
    warning-level path; every alert becomes a connection-closing CRYPTO_ERROR.

## §4.9 Discarding Unused Keys

- [x] 9001-023 (§4.9) The server shall send new data at the highest currently
  available encryption level.
  - test: `tests/crypto/promote_test.c` — `test_promote`
- [x] 9001-024 (§4.9.1) When the server first successfully processes a
  Handshake packet, it shall discard the Initial keys.
  - test: `tests/tls/keydiscard_test.c` — `test_keydiscard`
  - test: `tests/crypto/discard_driver_test.c` — `test_discard_driver`
- [x] 9001-025 (§4.9.1) After discarding Initial keys, the server shall not
  send Initial packets.
  - test: `tests/crypto/promote_test.c` — `test_promote`
- [x] 9001-026 (§4.9.2) When the handshake is confirmed, the server shall
  discard its Handshake keys.
  - test: `tests/tls/keydiscard_test.c` — `test_keydiscard`
- [ ] 9001-027 (§4.9.3) When the server has received a 1-RTT packet, it shall
  discard 0-RTT keys within a short time (three times the PTO is
  recommended).

## §5.1 Packet Protection Keys

- [x] 9001-028 (§5.1) The implementation shall derive the AEAD key, IV, and
  header protection key from the traffic secret with HKDF-Expand-Label using
  the labels "quic key", "quic iv", and "quic hp" and a zero-length Context.
  - test: `tests/crypto/hkdf_test.c` — `test_hkdf_expand_label`
  - test: `tests/tls/initial_test.c` — `test_initial_client`

## §5.2 Initial Secrets

- [x] 9001-029 (§5.2) The implementation shall compute the Initial secret as
  HKDF-Extract with salt 0x38762cf7f55934b34d179ae6a4c80cadccbb7f0a over the
  client's Destination Connection ID, expanding with "client in" and
  "server in" under SHA-256 (pinned to the Appendix A vectors).
  - test: `tests/tls/initial_test.c` — `test_initial_client`
  - test: `tests/tls/initial_test.c` — `test_initial_server`
- [x] 9001-030 (§5.2) When a Retry has been sent, the server shall derive
  subsequent Initial secrets from the connection ID it selected, kept
  distinct from the original Destination Connection ID it advertises.
  - test: `tests/tls/sdrv_test.c` —
    `test_sdrv_retry_advertises_true_odcid_not_key_derivation_dcid`

## §5.3 AEAD Usage

- [x] 9001-031 (§5.3) The server shall protect packets with the AEAD
  negotiated for the TLS connection.
  - test: `tests/transport/protectcs_test.c` — `test_protectcs`
  - test: `tests/transport/aead_suite_test.c` — `test_aead_suite`
  - test: `tests/tls/aead_params_test.c` — `test_aead_params`
- [x] 9001-032 (§5.3) The server shall not negotiate a cipher suite for which
  no header protection scheme is defined (TLS_AES_128_CCM_8_SHA256).
  - test: `tests/tls/cipher_test.c` — `test_cipher_supported`
- [x] 9001-033 (§5.3) The server shall not reject a ClientHello for offering
  cipher suites it does not support; unsupported suites are skipped during
  selection.
  - test: `tests/tls/cipher_test.c` — `test_cipher_select_falls_back`
- [x] 9001-034 (§5.3) The implementation shall form the AEAD nonce as the IV
  exclusive-ORed with the left-padded packet number, and shall use the QUIC
  header up to and including the unprotected packet number as the associated
  data.
  - test: `tests/transport/protect_test.c` — `test_protect_nonce_rfc`
  - test: `tests/transport/protect_test.c` — `test_protect_tamper`

## §5.4 Header Protection

- [x] 9001-035 (§5.4, §5.4.1) After packet protection, the implementation
  shall mask the low 4 (long header) or 5 (short header) bits of the first
  byte and the Packet Number field with a 5-byte mask derived from the
  ciphertext sample.
  - test: `tests/transport/hpapply_test.c` — `test_hpapply_byte0_long`
  - test: `tests/transport/hpapply_test.c` — `test_hpapply_byte0_short`
  - test: `tests/transport/hpapply_test.c` — `test_hpapply_pn`
  - test: `tests/transport/hp_test.c` — `test_hp_apply_roundtrip`
- [~] 9001-036 (§5.4) The implementation shall keep the header protection key
  unchanged for the duration of the connection, including across key updates.
  - evidence: the key-update derivation (`src/tls/keys/keyupdate/kuderive.c`)
    produces only the next packet-protection secret ("quic ku"); the header
    protection key is never re-derived.
- [x] 9001-037 (§5.4.2) The implementation shall take the 16-byte header
  protection sample starting 4 bytes after the start of the Packet Number
  field.
  - test: `tests/transport/hpsample_test.c` — `test_hpsample_offset`
- [x] 9001-038 (§5.4.2) The server shall discard packets that are not long
  enough to contain a complete sample.
  - test: `tests/transport/hpsample_test.c` — `test_hpsample_ok`
- [x] 9001-039 (§5.4.3) Where an AES-based AEAD is in use, the implementation
  shall compute the header protection mask as AES-ECB(hp_key, sample).
  - test: `tests/transport/hp_test.c` — `test_hp_mask`
  - test: `tests/transport/hp_suite_test.c` — `test_hp_suite`
- [x] 9001-040 (§5.4.4) Where AEAD_CHACHA20_POLY1305 is in use, the
  implementation shall compute the mask with raw ChaCha20 using sample bytes
  0..3 as the counter and 4..15 as the nonce over five zero bytes (pinned to
  the Appendix A.5 sample).
  - test: `tests/transport/hp_chacha_test.c` — `test_hp_chacha`
  - test: `tests/transport/hp_suite_test.c` — `test_hp_suite`

## §5.5 Receiving Protected Packets

- [x] 9001-041 (§5.5) If a packet cannot be successfully unprotected, then
  the server shall discard it.
  - test: `tests/transport/rxpacket_test.c` — `test_rxpacket_tampered_tag`
  - test: `tests/transport/rxpacket_test.c` — `test_rxpacket_wrong_key`

## §5.6 Use of 0-RTT Keys

- [~] 9001-042 (§5.6) The server shall not use 0-RTT keys to protect packets;
  acknowledgments of 0-RTT packets are protected with 1-RTT keys.
  - evidence: only the client-direction early traffic secret is derived
    (`tests/tls/early_test.c` — `test_early_server` covers the receive side),
    and responses to 0-RTT requests are deferred until 1-RTT send keys exist
    (interop testcase `zerortt`, see [test coverage](../test-coverage.md)).

## §5.7 Receiving Out-of-Order Protected Packets

- [ ] 9001-043 (§5.7) While the TLS handshake is not complete, the server
  shall not process incoming 1-RTT protected packets (it may store them for
  later decryption).

## §5.8 Retry Packet Integrity

- [x] 9001-044 (§5.8) The implementation shall compute the Retry Integrity
  Tag as AEAD_AES_128_GCM with the fixed key and nonce over the Retry
  Pseudo-Packet (ODCID-prefixed), pinned to the Appendix A.4 vector, and
  shall reject Retry packets whose tag does not verify.
  - test: `tests/tls/retry_tag_test.c` — `test_retry_tag_rfc9001_a4_vector`
  - test: `tests/tls/retry_drive_test.c` — `test_retry_process_bad_tag`

## §6 Key Update

- [x] 9001-045 (§6) The server shall detect a key update from a changed Key
  Phase bit and shall toggle the bit when initiating its own update.
  - test: `tests/tls/keyphase_test.c` — `test_keyphase`
  - test: `tests/tls/kuswitch_phasebit_test.c` — `test_kuswitch_phasebit`
  - test: `tests/tls/keyupdate_test.c` — `test_keyupdate_phase_tracks`
- [ ] 9001-046 (§6) The server shall not send TLS KeyUpdate messages and
  shall treat receipt of one as a connection error of type 0x010a
  (unexpected_message).
- [x] 9001-047 (§6.1) When initiating a key update, the implementation shall
  derive the next secret with HKDF-Expand-Label using the label "quic ku".
  - test: `tests/tls/kuderive_test.c` — `test_kuderive`
  - test: `tests/tls/kuswitch_derive_test.c` — `test_kuswitch_derive`
- [x] 9001-048 (§6.1) The server shall not initiate a key update before the
  handshake is confirmed.
  - test: `tests/tls/kudrive_trigger_test.c` — `test_kudrive_trigger`
  - test: `tests/tls/initiate_test.c` — `test_initiate`
- [x] 9001-049 (§6.1) The server shall not initiate a subsequent key update
  until a packet sent in the current key phase has been acknowledged.
  - test: `tests/tls/keyupdate_test.c` — `test_keyupdate_blocked_until_acked`
- [x] 9001-050 (§6.1) The server shall retain old keys until it has
  successfully unprotected a packet sent with the new keys.
  - test: `tests/tls/oldkey_test.c` — `test_oldkey`
  - test: `tests/tls/keyupdate_test.c` — `test_keyupdate_decrypt_window`
- [x] 9001-051 (§6.2) When a packet is successfully processed under the next
  key phase, the server shall update its send keys to that phase before
  acknowledging the packet.
  - test: `tests/tls/kudrive_recv_phase_test.c` — `test_kudrive_recv_phase`
  - test: `tests/tls/keyupdate_test.c` — `test_keyupdate`
- [x] 9001-052 (§6.3) The server shall be able to retain two sets of packet
  protection keys for receiving: the current and the next.
  - test: `tests/tls/keyupdate_test.c` —
    `test_keyupdate_two_adjacent_generations`
- [ ] 9001-053 (§6.3, §9.5) When responding to an apparent key update, the
  server shall not generate a timing side channel that reveals whether the
  Key Phase bit was valid.
- [ ] 9001-054 (§6.4) If protection is removed with old keys from a packet
  whose number is higher than packets protected with newer keys, then the
  server shall treat it as a connection error of type KEY_UPDATE_ERROR.
- [x] 9001-055 (§6.5) The server should wait three times the PTO before
  initiating a further key update and should discard old read keys after
  three times the PTO.
  - test: `tests/tls/kudrive_discard_timing_test.c` —
    `test_kudrive_discard_timing`
  - test: `tests/tls/initiate_test.c` — `test_initiate`
- [x] 9001-056 (§6.6) The server shall count packets encrypted under each key
  and shall initiate a key update before exceeding the AEAD confidentiality
  limit (2^23 for AES-GCM).
  - test: `tests/tls/aeadlimit_test.c` — `test_aeadlimit`
  - test: `tests/tls/kudrive_trigger_test.c` — `test_kudrive_trigger`
- [x] 9001-057 (§6.6) The server shall count received packets that fail
  authentication and shall close the connection with AEAD_LIMIT_REACHED when
  the AEAD integrity limit is exceeded.
  - test: `tests/transport/connloop_test.c` —
    `test_auth_fail_reaches_limit_aesgcm`
  - test: `tests/transport/connio_test.c` —
    `test_connio_close_on_aead_limit_wire_content`
  - test: `tests/transport/connrunner_test.c` —
    `test_advance_closes_on_aead_limit`

## §8 QUIC-Specific Adjustments to the TLS Handshake

- [x] 9001-058 (§8.1) The server shall use ALPN for protocol negotiation and
  shall close the connection with error 0x0178 (no_application_protocol) when
  no application protocol is negotiated.
  - test: `tests/tls/alpnver_test.c` — `test_alpnver_require`
  - test: `tests/tls/eebuild_test.c` — `test_eebuild_rejects_no_negotiation`
  - test: `tests/tls/negotiate_test.c` —
    `test_negotiate_none_when_neither_offered`
- [x] 9001-059 (§8.1) The server shall select an application protocol
  compatible with the QUIC version chosen by the client.
  - test: `tests/tls/alpnver_test.c` — `test_alpnver_compatible`
  - test: `tests/tls/alpnver_test.c` — `test_alpnver_protocol`
- [x] 9001-060 (§8.2) The server shall send the quic_transport_parameters
  extension (0x39) in its EncryptedExtensions.
  - test: `tests/tls/server_tp_test.c` — `test_server_tp_ids_and_values`
  - test: `tests/tls/tpext_test.c` — `test_tpext_roundtrip`
  - test: `tests/tls/sflight_encext_test.c` — `test_sflight_encext`
- [x] 9001-061 (§8.2) If a ClientHello lacks the quic_transport_parameters
  extension, then the server shall close the connection with an error of type
  0x016d (missing_extension).
  - test: `tests/tls/sdrv_test.c` —
    `test_sdrv_recv_client_hello_missing_tp_ext_rejected`
  - test: `tests/app/h3_loopback_test.c` —
    `test_srvboot_refusal_reports_missing_tp_ext`
- [x] 9001-062 (§8.3) If a CRYPTO frame is received in a 0-RTT packet, then
  the server shall treat it as a connection error of type
  PROTOCOL_VIOLATION.
  - test: `tests/transport/permit_test.c` — `test_permit_matrix`
- [ ] 9001-063 (§8.4) If a ClientHello carries a non-empty legacy_session_id
  field, then the server should treat it as a connection error of type
  PROTOCOL_VIOLATION.
  - gap: the server echoes the received session id
    (`test_sdrv_session_id_echo`) and rejects only overlong ids.

## §9 Security Considerations

- [x] 9001-064 (§9.2) The server shall apply anti-replay protection to 0-RTT:
  a ticket is accepted for 0-RTT only on first use.
  - test: `tests/transport/zerortt_seen_test.c` —
    `test_seen_replay_is_not_first_use`
  - test: `tests/tls/ticketguard_test.c` — `test_ticketguard_single_use`
  - test: `tests/tls/sdrv_test.c` — `test_sdrv_early_data_replay_rejected`
- [~] 9001-065 (§9.2) The server shall treat session tickets and address
  validation tokens as opaque carriers of connection state, not application
  semantics.
  - evidence: the sealed ticket carries only TLS/transport resumption state
    (`tests/tls/ticket_test.c` — `test_ticket_roundtrip` round-trips exactly
    those fields); no application data enters the ticket.
- [x] 9001-066 (§9.3) While the client address is unvalidated, the server
  shall not send more than three times the bytes it has received.
  - test: `tests/transport/antiamp_test.c` — `test_antiamp_budget`
  - test: `tests/transport/dosmitigate_test.c` — `test_dosmitigate`
- [ ] 9001-067 (§9.5) The implementation shall apply header protection
  removal, packet number recovery, and packet protection removal together
  without timing side channels.
- [x] 9001-068 (§9.7) The server shall generate protocol values such as
  connection IDs from a secure random source.
  - test: `tests/common/rng_test.c` — `test_rng_distinct_and_full`
  - test: `tests/common/cidgen_test.c` — `test_cidgen_distinct_and_full`

## Out of scope

Client-only or operator-level requirements this server SDK does not
implement, excluded from the coverage denominator:

- (§4.1.2) Client confirmation of the handshake on HANDSHAKE_DONE receipt or
  a 1-RTT acknowledgment — client behavior.
- (§4.1.4) Continuing to respond while asynchronous TLS processing completes
  — the TLS stack runs synchronously in-process; no waiting state exists.
- (§4.2) "Clients MUST NOT offer TLS versions older than 1.3" — client
  behavior.
- (§4.3) ClientHello size management (Retry-gated buffering of partial
  ClientHellos, minimizing ticket size) — MAY-level operational guidance.
- (§4.4) Client authentication of the server identity — client behavior.
- (§4.4) Client rejection of post-handshake CertificateRequest messages —
  client behavior.
- (§4.5) "Clients SHOULD NOT reuse tickets" — client behavior.
- (§4.6.1) Client rejection of a NewSessionTicket whose max_early_data_size
  is not 0xffffffff — client behavior.
- (§4.6.2) Client stream-state reset on 0-RTT rejection and treating a 0-RTT
  acknowledgment after rejection as an error — client behavior.
- (§4.9.1) Client discarding Initial keys when first sending a Handshake
  packet — client behavior (the server side is 9001-024).
- (§4.9.3) Client discarding 0-RTT keys upon installing 1-RTT keys — client
  behavior.
- (§5.1) "Other versions of TLS MUST provide a similar function" —
  requirement on future TLS versions, not an implementation.
- (§5.2, §9.6) Defining new salts and key-derivation labels for future QUIC
  versions — guidance to version designers; the v2 instantiation is covered
  by the RFC 9369 ledger.
- (§5.6) Client-side 0-RTT usage restrictions (no application data unless
  requested, stop sending on rejection, never decrypt received 0-RTT, none
  after 1-RTT keys are installed) — client behavior.
- (§5.7) Client deferral of 1-RTT processing and Finished-coalescing advice —
  client behavior.
- (§6.6) "Any TLS cipher suite specified for use with QUIC MUST define AEAD
  limits" — requirement on future specifications.
- (§8.1) Client use of error 0x0178 when ALPN negotiation fails — client
  behavior.
- (§8.2) Sending unsupported_extension when quic_transport_parameters appears
  outside QUIC — this SDK's TLS never runs outside QUIC.
- (§8.3) "Clients MUST NOT send the EndOfEarlyData message" — client
  behavior.
- (§8.4) Client prohibition on requesting TLS 1.3 middlebox compatibility
  mode — client behavior.
- (§9.2) Application-protocol 0-RTT usage profiles and replay analyses — an
  application-layer concern (HTTP/3's profile belongs to RFC 9114).
- (§9.3) Padding the ClientHello's datagram to the minimum size — client
  behavior.
- (§9.4) "Future header protection variants MUST use a PRF" — requirement on
  future specifications.
- (§10) IANA registration of the quic_transport_parameters codepoint —
  registry process, not implementation.
